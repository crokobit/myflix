%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - @follows.each do |follow|
        %tr
          %td
            %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest(follow.email.downcase)}?s=40")
            = link_to follow.name, user_path(follow)
          %td.extra-padding= QueueItem.where(user: follow).count
          %td.extra-padding= follow.followed_me_users.count
          %td.extra-padding
            = link_to follow_relationship_path(follow) , method: :delete do
              %i.glyphicon.glyphicon-remove

